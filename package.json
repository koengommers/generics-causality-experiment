{
  "name": "root",
  "private": true,
  "scripts": {
    "heroku-prebuild": "lerna bootstrap",
    "build": "lerna run build",
    "heroko-postbuild": "knex migrate:latest",
    "start": "node packages/server/dist/index.js"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "engines": {
    "node": "12.x"
  }
}
