{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build-all": "lerna run build",
    "postinstall": "npm run bootstrap && npm run build-all",
    "heroko-postbuild": "knex migrate:latest",
    "start": "node packages/server/dist/index.js"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "engines": {
    "node": "12.x"
  }
}
